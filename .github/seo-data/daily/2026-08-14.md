# SEO operations — 2026-08-14

## Current cycle

- Site: `https://agentsight.us/`
- Local timezone: `America/Los_Angeles`
- Cycle type: production closeout, product-drift repair, source/data patrol, and public-search verification
- Starting website main: `83e34ea6e789510815de8b250b99f305debe8ad9`
- Starting rendered product snapshot: AgentSight `v1.0.17`
- Current authoritative product release discovered today: AgentSight `v1.0.21`
- Current product source commit: `412ee6dbad967fa48a226a8052ee0c689e4bd1c6`
- Public URL removals, renames, redirects, canonical migrations, navigation changes, analytics-policy changes, and documentation-boundary changes selected today: none

## Completion of the previous rendered change

PR `#58`, **Sync v1.0.17 and document the session-first product workflow**, was squash-merged at `2026-08-14T16:52:24Z` as `83e34ea6e789510815de8b250b99f305debe8ad9`.

- Main Website CI run `31821362671`: successful.
- Publish static site run `31821362661`: successful.
- `site/.source-sha`: exactly `83e34ea6e789510815de8b250b99f305debe8ad9` after publication.
- `/product/` is a qualifying major evergreen refresh because it replaced the old product-level description with a source-pinned session-first workflow, portable-session versus Linux system-capture boundary, and current Node/Controller relationship rather than a version-label-only change.
- Independent canonical retrieval immediately after publication still exposed the earlier `v1.0.15` homepage generation. Because exact main CI, publication, and the production-branch source marker agree, this remains a crawler/CDN freshness qualification rather than evidence of a failed static publication. No cache-forcing rendered change is justified solely for that mismatch.

## Fresh product truth

A fresh GitHub Releases inspection found four product releases newer than the website snapshot:

- `v1.0.18` at `f0ffd56cc7fa70a1de5fc7b814c87521a811cf4f`: agent usage and session analysis;
- `v1.0.19` at `1885b064c5a1133a7d7b4ad5e97e3cd134bfa2d4`: multi-machine organization overview and browser regression coverage;
- `v1.0.20` at `f7d961f86d57073013a58a8f72ea0528e8e63c38`: release-workflow safety only;
- `v1.0.21` at `412ee6dbad967fa48a226a8052ee0c689e4bd1c6`: current release, including explicit Google/GitHub sign-in provider-status behavior and installation/startup documentation.

The drift is materially user-facing, not just a version-number difference:

- the signed-in hosted app now lands on **All machines** and aggregates bounded reachable-Node overviews in browser memory through Direct or Relay;
- Controller supplies machine directory/access policy but does not persist the fetched Node snapshots or the browser-produced fleet aggregate;
- machine/organization views expose observed Tokens, source-reported subscription windows, Agent Plans, CPU/RSS, and session state;
- observed token use and provider-reported capacity remain separate signals;
- a selected session now has three views — Conversation, Process Tree & AI Prompts, and Analysis — because Analysis replaces the old separate Timeline and Detailed Events tabs while retaining event inspection inside the timeline;
- supported hosted sign-in choices stay visible, with unconfigured providers disabled and explained rather than silently hidden.

The existing `/product/` and `/architecture/` canonicals already own these reader decisions. Creating thin v1.0.18/v1.0.19/v1.0.21 release pages would duplicate existing intent, so today’s repair expands those existing pages and synchronizes current metadata, changelog, `llms.txt`, and product-route sitemap dates.

## Analytics and source data

Runtime GA4 source remains configured with the documented path-only URL policy:

- property tag `G-VVRNSCMWBX` remains present in source;
- `page_location` is origin plus pathname;
- `page_path` is pathname;
- Google signals are disabled;
- ad-personalization signals are disabled.

The configured Drive folder still contains only:

- `2026-07-27_to_2026-08-02_ga4_source.json`;
- `2026-08-03_to_2026-08-09_ga4_source.json`.

There is still no paired `*_ga4_organic_landing_pages.csv` and no matching Search Console CSV. The `2026-08-03` through `2026-08-09` window is finalized under the three-day lag, so this remains an exporter-health failure. GA4/GSC source-native metrics are unavailable, not zero. The existing Apps Script human-only blocker remains valid.

Cloudflare analytics remains disabled by site configuration, so no Cloudflare source-native traffic metric is reported.

## Public search and off-site visibility

A bounded public search patrol used the configured canonical-domain and AgentSight brand query families.

- The canonical homepage is discoverable in public search, but sampled search results are substantially behind current releases.
- The product repository and Eunomia documentation remain prominent brand results.
- The published AgentSight paper is discoverable on arXiv.
- No new independent external link to `agentsight.us` was verified in this scan. Search results or snippets alone are not classified as backlinks.
- Direct canonical homepage retrieval during this cycle still returned the earlier `v1.0.15` generation while the exact publication branch already identified `v1.0.17`. Treat this as freshness evidence, not as a route/indexability repair target unless it remains reproducible after the new exact publication.

## Shared SEO skill

- Consuming-repository gitlink: `f42128a3f05c73cf10c786a2711c488bb3a14839`.
- Current allowed upstream `main`: the same `f42128a3f05c73cf10c786a2711c488bb3a14839`.
- No submodule update is needed today.

## Selected rendered outcome

Fresh branch: `seo/agentsight-2026-08-14-v1-0-21-fleet-analysis`.

The selected coherent outcome is **synchronize AgentSight v1.0.21 and correct the product/fleet/session-analysis model**. Intended rendered scope:

- shared current release/commit metadata;
- `/product/` fleet → Node → session workflow, source-reported capacity boundary, three-view session workspace, portable-versus-Linux capture boundary, and sign-in-status behavior;
- `/architecture/` browser-side fleet aggregation and Node/Controller data boundary;
- `/changelog/` current release highlights;
- `public/llms.txt` machine-readable product summary;
- product-facing sitemap freshness dates;
- today’s operating record and durable status.

Preserved behavior: all routes and canonical owners, redirects, primary navigation, pricing catalog semantics, GA4 implementation, documentation ownership, dependencies, workflows, and deployment configuration.

Rollback point: website main `83e34ea6e789510815de8b250b99f305debe8ad9`.

## Content clock

The successful `/product/` publication from PR `#58` completed at approximately `2026-08-14T16:53Z` and is the latest completed qualifying substantive publication at the start of this cycle. The current v1.0.21 correction also contains substantial source-grounded Product and Architecture additions, but it does not count until exact CI, squash merge, static publication, and acceptance are complete.

## Blockers

- Google SEO export pipeline: unchanged genuine human-only blocker; finalized GA4/GSC CSV output is still absent and this runtime cannot execute or configure the external Apps Script exporter.
- Scoped npm first publication: unchanged genuine human-only blocker; do not claim `@eunomia-bpf/agentsight` installability until an npm administrator completes/validates first publication.
