# SEO operations — 2026-08-20

## Current cycle

- Site: `https://agentsight.us/`
- Product repository: `eunomia-bpf/agentsight`
- Product release: AgentSight `v1.0.25`
- Product source commit: `0080545f7c6b110ec2d4a4af5100b58f514c84d5`
- Website starting `main`: `aa71b3b7429bff9a550f1cd3d5753736ce4021c4`
- Shared SEO skill pointer: `f42128a3f05c73cf10c786a2711c488bb3a14839`; allowed upstream `main` is unchanged at the same commit
- Cycle result: no rendered site change is justified today; deliver the required data/status refresh only
- Public URL additions, removals, renames, redirects, canonical migrations, navigation changes, and analytics-policy changes: none

## Repository, product, and skill truth

No earlier open `seo/agentsight-` pull request remains active. The cycle started from the current remote `main` and uses a fresh `seo/agentsight-2026-08-20-daily-ops` branch.

The authoritative AgentSight product `master` still ends at `0080545f7c6b110ec2d4a4af5100b58f514c84d5`, whose release commit is `chore: release agentsight 1.0.25 [skip ci]`. No newer product commit or release is present, so there is no release or product-source drift to synchronize today.

The shared SEO skill allowed branch still ends at `f42128a3f05c73cf10c786a2711c488bb3a14839`, exactly matching the website's current gitlink. No compatible skill update is available.

## Analytics implementation and production source

Runtime GA4 remains present in `src/app/layout.tsx` with measurement ID `G-VVRNSCMWBX`. The configuration continues to report `page_location` as origin plus pathname and `page_path` as pathname, omitting query strings under the site's path-only policy. Google signals and ad-personalization signals remain disabled.

The generated production branch also contains the GA loader. `site/.source-sha` still identifies the latest substantive rendered squash commit `2c353664d149079b1e5b38a67d5204ac2e724e14`, which published the refreshed MCP server audit. No publication-branch drift was found.

## Google Analytics and Search Console evidence

The configured Drive folder `agentsight.us SEO Weekly CSV` resolved uniquely and was inspected read-only.

- Configured finalization lag: three days.
- Latest fully finalized calendar date today: `2026-08-17`.
- Present AgentSight source manifests remain exactly:
  - `2026-07-27_to_2026-08-02_ga4_source.json`
  - `2026-08-03_to_2026-08-09_ga4_source.json`
  - `2026-08-10_to_2026-08-16_ga4_source.json`
- The newest manifest covers `2026-08-10` through `2026-08-16`, was generated on 17 August, and still has public-safe SHA-256 `f8d31e5f2ecc4f317ec000d977a004f691c12473a8579cd144a9ac6403f44b44`.
- Direct-child inspection still finds no CSV files. There is no paired `*_ga4_organic_landing_pages.csv` for any manifest and no matching `*_gsc_*.csv` export.
- No newer completed weekly export window is expected yet for 17–23 August; the actionable defect is that the already-finalized 3–9 August and 10–16 August windows remain incomplete.

The exporter-health failure is therefore unchanged but independently reconfirmed: two consecutive fully finalized weekly windows have GA4 source manifests without usable GA4 landing-page CSV or Search Console CSV artifacts. Source-native traffic and search metrics remain unavailable, not zero, and there is no valid period-over-period comparison to compute.

The current runtime can inspect the Drive artifacts but cannot execute or configure the external Google Apps Script exporter. The existing human-only blocker remains accurate and does not require a new repository-side workaround.

Cloudflare analytics remains not configured.

## Public search and production visibility

The bounded public visibility check continues to find the canonical `agentsight.us` domain, the Eunomia product documentation, and the Eunomia GitHub repository in public search results. Search composition remains noisy because unrelated products and stale crawls use the AgentSight name.

The independent crawler is still behind the exact publication branch: its homepage snapshot exposes an older v1.0.3 generation, while fresh retrieval of the MCP audit and Agent Flamegraph detail routes could not be established through that crawler in this cycle. This is directional crawler/index/cache evidence, not proof of a production rollback. The exact publication branch, source marker, and generated HTML remain internally consistent, so no rendered cache-forcing change is justified.

A fresh public search still does not establish that `@eunomia-bpf/agentsight@1.0.0` is available from the npm registry. The existing first-publication blocker therefore remains unresolved; unrelated packages with similar names do not count as resolution.

No new independently verified backlink to `agentsight.us` was established in this bounded scan.

## Content clock and decision

The latest qualifying substantive publication remains the MCP server audit evergreen refresh from PR `#72`, whose exact static publication completed at `2026-08-19T17:07:34Z` (10:07 PDT).

Waiting until the next normal daily operating cycle around 09:00 PDT on 21 August remains inside the rolling 48-hour publication target. Product, production-source, analytics-instrumentation, and crawlability checks found no higher-priority site-owned defect with a safe repository repair. The preferred next substantive candidate also depends on public-safe first-party runtime/session data and should not be manufactured merely to create activity.

Therefore this is a valid no-rendered-change day. The coherent outcome is the required daily evidence/status refresh only. It does not reset the 48-hour substantive-content clock.

## Human-only blockers

1. **Google SEO export pipeline:** unchanged and reconfirmed. Two consecutive fully finalized weekly windows remain manifest-only. An authorized Apps Script operator must inspect exporter health, correct the missing CSV generation path, rerun one finalized window, and verify the complete GA4 + Search Console artifact set.
2. **npm scoped first publication:** unchanged. An npm administrator must complete or authorize the first `@eunomia-bpf/agentsight` publication and any required trusted-publisher setup before the website may claim the scoped npm package as an install path.

## Delivery plan

This cycle changes only `.github/seo-data/daily/2026-08-20.md` and `status.md`. It does not change rendered output or the protected operating control plane. The branch will use a real non-draft pull request, ordinary Website CI, a complete post-CI review, and squash merge. Because this is a no-site-change day, no redundant metadata closeout pull request is required after the data/status PR merges.
