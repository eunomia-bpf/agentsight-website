# AgentSight SEO daily — 2026-09-13

## Operating state

- Canonical site: `https://agentsight.us/`.
- Cycle base: `main` at `6d54f9fc2332d591e3eba5f47ea491feec03631b`.
- No earlier open `seo/agentsight-` pull request was present when the cycle began.
- Working branch: `seo/agentsight-2026-09-13-otel-export`.
- Shared SEO skill pointer remains `f42128a3f05c73cf10c786a2711c488bb3a14839`; allowed upstream `main` still resolves to the same commit, so no submodule update is due.
- No site-owned `autoaction.md` exists; the normal PR/CI/deployment lane remains authoritative.
- Product truth remains AgentSight `v1.0.31` at `bb99b66f8f98e4b9f8b1769a3da0a8fbbe26b6c3`. The latest GitHub release is still `v1.0.31`; no product/release drift was found.

## Source and production patrol

The canonical homepage and Blog were directly readable during this cycle and identify the current product as AgentSight `v1.0.31`. The Blog now directly exposes the 11 September token-reconciliation article, and following the Blog link returns the full current article. This clears the direct-retrieval freshness qualification recorded immediately after that publication; no cache-forcing repair is justified.

Baseline public search used `site:agentsight.us`, the canonical domain, the AgentSight brand, and the newest publication title. Search remains an incomplete discovery surface, so search result presence/counts are not treated as KPI data. No new independently verified canonical backlink or reproducible crawl/indexing defect was established in this pass.

## Google export evidence

The configured Drive folder remains readable. The source-generation state is unchanged:

- `2026-08-17_to_2026-08-23`: no export family is present.
- `2026-08-24_to_2026-08-30`: the GA4 source manifest still has the original `2026-08-31T16:05:46Z` generation time; no post-lag regeneration is present.
- `2026-08-31_to_2026-09-06`: the GA4 source manifest still has the original `2026-09-07T16:05:15Z` generation time; no post-lag regeneration is present.
- `2026-09-07_to_2026-09-13`: no completed-window family is present yet, which is expected because the local reporting window has not finished at patrol time.

Because there is still no new finalized export, this cycle does not recompute week-over-week KPIs. The newest previously observed 8/31–9/6 files remain directional only: GA4 listed 12 sessions / 11 active users, while GSC listed 4 clicks / 260 impressions / 1.54% CTR with date rows through 9/5 and no 9/6 row. Those values are carried only as the last observed snapshot, not promoted to finalized performance data.

The existing human-only Apps Script blocker remains genuine: backfill 8/17–8/23 and regenerate 8/24–8/30 plus 8/31–9/6 after the configured finalization lag.

## Content SLO decision

The latest qualifying substantive publication before this cycle is `/blog/how-agentsight-reconciles-token-usage/`, whose exact production publication completed at `2026-09-12T16:08:44Z` (09:08:44 PDT). Its rolling deadline is **14 September 2026 at 09:08:44 PDT**.

A metadata-only day would leave less than ten minutes between the normal next-day cycle start and that deadline, while the operating cadence is not guaranteed to start at the exact minute. Because the repository contract requires publication when the next scheduled cycle could breach the rolling window, this cycle publishes a substantive page rather than relying on that narrow margin.

## Research brief: OpenTelemetry GenAI export semantics

- Proposed title: **How AgentSight exports OpenTelemetry GenAI spans**
- Slug: `/blog/how-agentsight-exports-opentelemetry-genai-spans/`
- Page type: implementation deep dive / telemetry-export evidence guide.
- Reader question: when AgentSight says it can export captured model calls to OpenTelemetry, exactly which AgentSight evidence becomes an OTel span, how are trace IDs and attributes chosen, what content leaves the machine, and which evidence remains local?
- Decision supported: whether an existing OTel collector can consume the needed model-call projection, and when the engineer must retain the AgentSight artifact because process/file/network/resource/provenance evidence is outside that projection.

### Semantic-overlap audit

`/compare/opentelemetry/` already owns the broad standards/product decision: OpenTelemetry is the wider telemetry infrastructure and AgentSight can complement it. `/blog/system-boundary-observability/` owns the multi-layer observability boundary. Neither page documents the v1.0.31 exporter transformation from a completed materialized `llm_call` into OTLP/HTTP JSON, trace-ID selection, endpoint precedence, content opt-in, asynchronous delivery behavior, or the current CLI-surface distinction between `debug trace` and `record`.

The new page therefore answers a distinct implementation/evidence question. It links to those canonical boundary pages rather than duplicating their comparisons.

### Primary-source findings

The pinned v1.0.31 source establishes the following:

- `collector/src/cmd_trace.rs` attaches `OtelExporter` as a sink on the shared `MaterializingAnalyzer`; correlation/token extraction happen before the exporter consumes a call.
- `ext/analysis/src/sinks/otel.rs` exports only completed materialized LLM calls, creates one `chat {model}` CLIENT span per call, and skips rows with no end timestamp.
- Trace IDs reuse explicit conversation/thread identity first, then AgentSight session identity, then one recording-scoped fallback; generic response IDs are not promoted to conversation IDs. Parent/child model-call spans are not inferred.
- Exported attributes cover the GenAI operation/provider/conversation/request/response/usage/finish-reason fields, `server.address`, and HTTP response status when available. HTTP status >=400 marks the span ERROR.
- Prompt/completion material is opt-in through `--otel-capture-content`; it is not exported by default.
- `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT` is used as-is. Otherwise the explicit CLI/base endpoint, `OTEL_EXPORTER_OTLP_ENDPOINT`, or localhost:4318 is treated as a base and `/v1/traces` is appended. The current sink uses OTLP/HTTP JSON.
- OTel posts are spawned asynchronously and failures are warnings; local materialization and collector acceptance are separate facts.
- In v1.0.31 the public `--otel`, `--otel-endpoint`, and `--otel-capture-content` switches live on `agentsight debug trace`. The normal `agentsight record` CLI does not expose those flags even though both paths share trace infrastructure.
- Current product documentation explicitly says tool/workflow spans are not emitted yet; the sink also does not map AgentSight process/file/network/resource rows or AgentSight provenance/confidence into these GenAI spans.

The OpenTelemetry OTLP specification independently confirms `/v1/traces` as the default OTLP/HTTP trace path and the distinction between base and signal-specific endpoint configuration. The GenAI attribute registry separately warns that input/output message attributes can contain sensitive information.

### Acceptance and rollback

Affected routes are the new article, `/blog/`, and `sitemap.xml`. Representative unaffected behavior is the existing OpenTelemetry comparison and the prior token-reconciliation article. Acceptance requires the built article to expose the intended title, canonical, TechArticle JSON-LD, source links, current v1.0.31 wording, Blog card, and sitemap route without changing unrelated public pages. Rollback is the rendered squash commit for this publication.

## Implementation scope

This branch adds one substantive article, one Blog hub card / CollectionPage entry, and one sitemap entry/Blog last-modified advance. No product, pricing, navigation, workflow, protected control-plane, analytics, or unrelated content changes are intended.

Local `npm run verify` cannot be executed on this operator host because outbound GitHub dependency resolution is unavailable. The ordinary exact-head Website CI remains the authoritative validation gate; the PR may not merge unless all expected checks succeed and a new from-scratch final review is clean.

## Delivery

Delivery is in progress through the normal non-draft PR, exact-head CI, final-review, squash-merge, exact-production, public-verification, and metadata-closeout lifecycle. Exact PR, commit, workflow, production timestamp, verification result, and the new 48-hour deadline will be written back in the metadata closeout after publication.