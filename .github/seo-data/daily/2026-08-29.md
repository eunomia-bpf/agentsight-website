# 2026-08-29 AgentSight SEO operating record

## Scope and baseline

- Canonical site: `https://agentsight.us/`.
- Cycle baseline: website `main` commit `1db8b1cfc58af4d7536d8413cab83fc7a3a3ec83`.
- No earlier open `seo/agentsight-*` pull request was present at cycle start.
- Shared SEO skills remain pinned to `f42128a3f05c73cf10c786a2711c488bb3a14839`; the allowed upstream `main` still equals that commit, so no skill-pointer update is available.
- Authoritative AgentSight `master` still equals release commit `934f441eff8ca210807333633f47b2efcb8cd020` (v1.0.30). No product-release drift was found.

## Analytics and source freshness

- The configured Drive folder resolves uniquely as `agentsight.us SEO Weekly CSV`.
- Direct folder enumeration on 29 August still finds no artifact matching the now-final `2026-08-17_to_2026-08-23` window.
- The newest available families remain `2026-08-10_to_2026-08-16_*`, generated on 17 August, only the morning after that window ended and before the configured three-day finalization lag. They remain directional rather than finalized evidence.
- The unchanged 10–16 August Search Console date snapshot still omits 16 August; the prior public-safe directional aggregate remains 5 clicks / 189 impressions, 2.65% CTR, weighted average position about 14.18.
- The unchanged 10–16 August GA4 landing-page snapshot still records 19 homepage sessions and 1 `/guides/` session. It is not promoted to a finalized KPI.
- Missing finalized data is not interpreted as zero. The external Apps Script post-lag refresh remains a human-only blocker.
- Cloudflare analytics remains not configured.

## Runtime analytics and public evidence

- `src/app/layout.tsx` still sends GA4 page location as origin + pathname and `page_path` as pathname; Google signals and ad-personalization signals remain disabled. No analytics implementation defect was found.
- Public retrieval of the homepage succeeds and shows AgentSight v1.0.30.
- Public baseline search continues to surface `agentsight.us`, while generic AgentSight searches remain ambiguous with unrelated products using the same name.
- Search/retrieval still does not establish indexing of `/blog/how-agentsight-direct-node-credentials-work/`, and immediate search after today's publication does not yet surface `/blog/when-agentsight-works-without-ebpf/` either. Exact publication workflows, the `site/.source-sha` marker, generated routes, Blog index, and sitemap are internally consistent, so these remain indexing/retrieval freshness qualifications rather than confirmed production outages.

## Factual defect and same-cycle repair

- The cycle found the live homepage `agentsight bind` card describing a **“process-lifetime access key.”** AgentSight v1.0.30 source contradicts that lifetime: `collector/src/cmd_bind.rs` reads the bootstrap key from the AgentSight config directory, creates a private file when absent, reuses it later, and prints that the access key survives Node restarts.
- Focused rendered PR `#93` changed only the stale product sentence plus the required operating records. The new copy identifies a locally stored bootstrap key and the scoped Direct capability minted during browser pairing while preserving the route, CTA, layout, analytics, navigation, and v1.0.30 release identity.
- Exact-head Website CI run `33264158595` passed for head `fb5434c9df8e37d127f2e3f4565c251bfa316209`.
- A from-scratch post-CI review confirmed the three-file diff, source contract, mergeability, public acceptance boundary, and no unrelated rendered changes.
- PR `#93` was squash-merged as `865e7087aba48ff344bce7436f9a0aab55f235f6`.
- Exact `Publish static site` run `33264205190` completed successfully for that squash commit at `2026-08-29T16:54:50Z`; the exact production branch marker advanced to the same commit.
- Immediate and later public crawler/search verification still returned the pre-repair sentence even after the exact publication marker advanced. Because the exact publish workflow and generated `site` state agree, this is recorded as public cache/retrieval freshness rather than a source/deployment rollback. A later cycle should retry the visible sentence.

## Publication research brief

### Candidate and reader decision

- Title: **When AgentSight works without eBPF—and when it does not**.
- Slug: `/blog/when-agentsight-works-without-ebpf/`.
- Reader: an engineer evaluating AgentSight on macOS, Windows, or unprivileged Linux, or deciding whether a particular investigation needs a privileged Linux recording.
- Problem: current product pages correctly mention that several commands can use native agent sessions without eBPF, but the site had no focused source-level decision guide separating what native-session evidence can establish from what independent eBPF capture can establish.
- Decision: use native `top` / `bind` / `vis` / relevant `report` workflows when the agent-native state owns the fact; use `record` or eBPF-backed debug when the machine/runtime boundary must independently establish the fact.

### Semantic overlap audit

- `/blog/system-boundary-observability/` owns the broad native-telemetry-versus-system-observation architecture comparison; it does not own the practical AgentSight command/privilege selection decision.
- `/blog/replay-coding-agent-repository-changes/` owns Agent Nebula replay methodology only.
- Integration/use-case pages mention individual no-sudo paths, but none centralizes the current v1.0.30 no-eBPF command set, fallback behavior, and evidence-strength boundary.
- The candidate therefore has information gain rather than restating an existing canonical page.

### Source table and claims boundary

- AgentSight v1.0.30 README: `top`, `bind`, `vis`, and `report` can use agent-native session files without eBPF on Windows, macOS, or Linux; `record` and eBPF-backed debug are Linux capture workflows.
- v1.0.30 usage guide: unprivileged `top` falls back to process snapshots and native sessions; `record` starts SSL/process/system/web-view collection; `bind` presents live host/native-session or saved-DB evidence; `vis` reads local Claude/Codex/Gemini history without sudo.
- v1.0.30 native analysis/session parser source: native-session evidence is a separate input path rather than a disguised kernel event stream.
- Claims deliberately avoided: native sessions are not described as independent system evidence; `bind` is not described as starting recording; provider CLIs are not claimed to expose identical native fields; no-eBPF mode is not claimed to capture all descendant filesystem/network effects.

## Substantive publication delivery

- Rendered PR `#94`, **content: explain when AgentSight works without eBPF**, delivered the source-level guide, the Blog index card/CollectionPage entry, sitemap entry, and this operating-record continuation.
- Exact head: `1d95cfc343f46e3db90be277dfa8b2047dd6718a`.
- Exact-head Website CI run `33264419403` passed. Its `verify` job completed the autonomous SEO scope guard, `npm ci`, `npm run verify`, and static-site artifact upload successfully.
- The exact CI artifact `static-site` was inspected before merge. It contains the new article route with the expected title, description, canonical, OpenGraph metadata and TechArticle JSON-LD; the Blog index contains the new card and CollectionPage member; generated `sitemap.xml` contains the new route.
- A from-scratch post-CI review rechecked the four-file PR scope, authoritative v1.0.30 README/usage contracts, generated output, semantic-overlap boundary, claims-to-avoid list, and mergeability. No blocker remained.
- PR `#94` was squash-merged as `d99642e923ad5bad35c06fbeaaf7451387a1f593`.
- Exact `Publish static site` run `33264487636` completed successfully for that exact squash commit at `2026-08-29T17:01:17Z` (10:01:17 PDT). Exact push Website CI run `33264487646` also passed.
- Production branch `site/.source-sha` exactly equals `d99642e923ad5bad35c06fbeaaf7451387a1f593`.
- Generated production `blog/when-agentsight-works-without-ebpf/index.html` contains the expected title, description, canonical and TechArticle JSON-LD. Generated `blog/index.html` links the article and generated `sitemap.xml` includes the route with `2026-08-29` last-modified value.
- Immediate public search does not yet discover the new route and direct crawler retrieval of the Blog surface can return a cache miss. This is a truthful public freshness qualification, not a publication failure, because exact CI, exact publish, production source marker, generated article, Blog index and sitemap all agree.

## Content clock

- New qualifying publication: `/blog/when-agentsight-works-without-ebpf/`.
- Exact publication time: `2026-08-29T17:01:17Z` (10:01:17 PDT).
- New rolling 48-hour deadline: `2026-08-31T17:01:17Z` (10:01:17 PDT).
- Today's publication reset the SLO well before the previous `2026-08-30T16:53:03Z` deadline.

## Human-only blockers

- Google Apps Script still needs a post-three-day-lag refresh for the completed 17–23 August GA4/GSC window.
- The scoped npm first publication remains independently unverified and must not be advertised as an install path.
