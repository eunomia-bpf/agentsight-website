# AgentSight SEO daily report — 2026-09-16

## Operating state

- The cycle began with one existing `seo/agentsight-*` PR, `#129`, carrying the unfinished 15 September OpenTelemetry evergreen refresh. The one-cycle concurrency rule therefore required finishing that work rather than opening a competing rendered branch.
- The authoritative website `main` advanced through unrelated dependency maintenance while `#129` was open. The final review compared the complete PR diff against the current mainline and found no semantic conflict with that dependency-only drift.
- The shared SEO skill pointer remains `f42128a3f05c73cf10c786a2711c488bb3a14839`, and the allowed `AutoArchive/seo-skill` upstream `main` still resolves to the same commit.
- Authoritative AgentSight product state remains v1.0.31 at `bb99b66f8f98e4b9f8b1769a3da0a8fbbe26b6c3`; both the latest release and product `master` still resolve there. No product-release drift was found.

## Completed rendered outcome

The existing canonical `/compare/opentelemetry/` page received the major evergreen refresh prepared in `#129`. The final page now reflects the released v1.0.31 exporter rather than the stale v1.0.3 description. It makes the current boundary explicit: completed materialized LLM calls can be projected to OTLP/HTTP GenAI spans, while process/file/network/resource evidence, tool/workflow rows, and AgentSight provenance are not automatically converted into those spans. It also documents trace-grouping precedence, detached asynchronous delivery, content-export privacy, and the need to verify Collector receipt separately from local materialization.

Final head `28eda43530efb66730ea1d7868e1e42aedef5106` had green exact-head Website CI, GitGuardian, and Cloudflare Pages, no unresolved review thread, and a source-grounded final review. PR `#129` was squash-merged as `89669d1e8a53b6f8ed9c8bde44eab7c31eb36817`.

Exact `Publish static site` run `35121801735` succeeded for that exact squash commit. The publish job completed at `2026-09-16T16:26:15Z` (09:26:15 PDT), 9 minutes 6 seconds before the prior rolling deadline. Production `site/.source-sha` exactly matches `89669d1e8a53b6f8ed9c8bde44eab7c31eb36817`. The next rolling 48-hour publication deadline is `2026-09-18T16:26:15Z` (09:26:15 PDT).

Generated production HTML for the canonical comparison contains the refreshed v1.0.31 lede and sections, one canonical URL, index/follow metadata, the GA4 bootstrap, current primary-source links, and the intended related-reading links. A public crawler verification at `2026-09-16T16:36:41Z` (09:36:41 PDT) still returned the pre-refresh v1.0.3 body while the production branch already contained the new HTML. The homepage remained publicly retrievable on v1.0.31. Treat this as a retrieval/CDN freshness qualification, not a production build incident; recheck the canonical page on the next cycle before clearing it.

Metadata-only closeout is carried by PR `#130` (`seo/agentsight-2026-09-16-closeout`). Its scope is limited to the 15–16 September daily records, `status.md`, and `block.md`; it does not change rendered source or scheduling state.

## Analytics and measurement

Direct enumeration of the configured Drive folder on 16 September found no files modified after 15 September and still found no `2026-08-17_to_2026-08-23` artifact family. The existing `2026-08-24_to_2026-08-30` and `2026-08-31_to_2026-09-06` families remain their next-morning snapshots without a post-lag refresh.

The `2026-09-07_to_2026-09-13` family is still the snapshot generated on 14 September around 09:05 PDT. It has now reached the site's configured three-day finalization boundary without a regenerated family. Its current directional rows remain:

- GA4 organic landing pages: 6 listed sessions, 4 active users, 0 key events; 5 sessions / 4 active users on `/` plus one blank landing row.
- GSC dates: 3 clicks / 375 impressions / 0.80% CTR, impression-weighted average position about 8.43 across 7–12 September; 13 September is still absent.

Because the snapshot was generated before the lag elapsed and still omits the boundary date in GSC, it is not promoted to finalized week-over-week KPI evidence. The human-only exporter repair therefore now covers four needs: backfill 17–23 August and post-lag refresh 24–30 August, 31 August–6 September, and 7–13 September.

## Public visibility and runtime checks

- Baseline public search continues to find the canonical AgentSight homepage and existing technical pages. The shared AgentSight name remains ambiguous because unrelated products also use it, so brand-result counts are not treated as a KPI.
- The canonical homepage remains publicly retrievable on v1.0.31, and representative unaffected comparison/integration pages remain retrievable after the publication.
- Repository runtime inspection still shows the GA4 bootstrap in `src/app/layout.tsx`, including pathname-based `page_location`/`page_path`, and the generated production comparison HTML includes the same Google tag. The Drive exports demonstrate continuing source-native collection. No analytics-code defect was found.
- Cloudflare traffic analytics remain intentionally disabled by policy; Cloudflare Pages status is CI/deployment evidence only.

## Decision

One substantive rendered outcome was completed and published within the rolling SLO, so no second speculative content change was started. The remainder of this cycle is metadata closeout through PR `#130`: record the exact publication and public-verification evidence, expand the existing Google exporter blocker to the newly matured 7–13 September window, refresh `status.md`, and leave rendered production identity unchanged.
