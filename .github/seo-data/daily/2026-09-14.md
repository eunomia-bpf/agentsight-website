# AgentSight daily SEO report — 2026-09-14

## Decision and operating window

This cycle starts from website `main` commit `4614b8a7e36f3cc61c88bdb112364d74a2489655`. No earlier open `seo/agentsight-` pull request exists, so the single-cycle concurrency slot is free. The pinned shared SEO skill remains `f42128a3f05c73cf10c786a2711c488bb3a14839`, and allowed upstream `AutoArchive/seo-skill` `main` still points to the same commit.

The latest qualifying production publication before this cycle is `/blog/how-agentsight-exports-opentelemetry-genai-spans/`, exact production completion `2026-09-13T16:36:21Z` (09:36:21 PDT). Its rolling 48-hour deadline is `2026-09-15T16:36:21Z` (09:36:21 PDT). Because the next daily operating window may execute after that deadline, a qualifying substantive publication is required in this cycle rather than deferring content to 15 September.

The selected outcome is a new source-level article, `/blog/how-agentsight-normalizes-agent-session-data/`. It owns the normalization step after provider-native transcript discovery: the common `AgentSession` / `SessionEvents` IR, preserved prompt/tool/path/response/usage/plan semantics, provider/source identity, and the fields the parser deliberately does not invent. This is distinct from `/blog/how-agentsight-discovers-local-agent-sessions/`, which owns discovery/index/cache behavior, and `/blog/how-agentsight-reconciles-token-usage/`, which owns report-time precedence across overlapping token observations.

## Product truth and research

The authoritative product remains AgentSight **v1.0.31** at commit `bb99b66f8f98e4b9f8b1769a3da0a8fbbe26b6c3`, released 5 September 2026. No product or release drift was found before implementation.

Primary-source findings used by the article:

- `docs/agent-session.md` defines `agent-session` as a reusable Rust library for native coding-agent session data, a common local IR, and live process-tree/session matching. It explicitly excludes direct OpenTelemetry export, UI/report rendering, database schema, and eBPF capture.
- `ext/session/src/types.rs` keeps provider/session identity around normalized `UserPrompt`, `ToolEvent`, `ToolPath`, `LlmResponse`, `PlanStep`, token-usage, and session metadata rather than flattening them into generic text events.
- `ToolPath` keeps an access semantic and optional rename source; `LlmResponse` keeps a source-native completion ID for merging split source records; prompts, tools, and responses retain prompt indexes and source-visible semantic fields when present.
- `ext/session/src/parser.rs` dispatches the supported native formats into provider-specific parsers before producing the common IR. The article does not re-own the existing discovery article's filesystem-root, bounded-index, cache, or Cursor-freshness material.
- `agentpprof` directly reuses shared `agent-session` prompt/tool/response types, demonstrating why parser semantics are useful as a library boundary rather than being copied into every consumer.

No benchmark, billing, complete-causality, or host-effect claim is inferred from transcript normalization. Missing native fields remain missing evidence; independent system capture remains a separate layer.

## Analytics and search evidence

Runtime GA4 source remains present with measurement ID `G-VVRNSCMWBX` and the repository's path-only URL policy. No source-level analytics removal or policy drift was found.

A new Drive family, `2026-09-07_to_2026-09-13`, appeared at approximately 09:05 PDT on 14 September. It is a next-morning export and remains inside the configured three-day finalization lag, so all numbers below are directional only:

- GA4 landing rows list **6 sessions**, **4 active users**, and **0 key events**: 5 sessions / 4 active users on `/`, plus one blank landing row.
- GSC date rows currently cover **7–12 September only**; 13 September is absent. The available rows total **3 clicks / 375 impressions / 0.80% CTR**, with weighted average position about **8.43**.
- The homepage contributes 3 clicks / 169 impressions. Among non-home pages, the existing local-session discovery article has 30 impressions at average position about 6.87; `/blog/` has 49 impressions; the Cursor integration has 17; the Agent Flamegraph guide has 15. These are preliminary signals, not finalized week-over-week evidence.

The older Google exporter blocker remains unchanged. `2026-08-17_to_2026-08-23` is still absent. `2026-08-24_to_2026-08-30` still has only its 31 August next-morning generation, and `2026-08-31_to_2026-09-06` still has only its 7 September next-morning generation. Both older completed windows are beyond the configured lag without post-lag refresh, so finalized week-over-week analysis remains blocked on the Apps Script owner.

Public-search sampling still returns the canonical AgentSight homepage and existing indexed technical pages. Immediately after publication, exact-title search had not yet surfaced the new session-normalization article; direct public retrieval subsequently succeeded, so this is ordinary index freshness rather than a publication incident.

## Known content drift

`/compare/opentelemetry/` still exposes an older v1.0.3 evidence label/source snapshot even though the current product is v1.0.31. The page's broad decision remains valid, but its “current export” wording is stale. It remains a separate evergreen refresh because this cycle's article owns native-session normalization rather than comparison semantics. Do not create a second renderer for the same comparison slug: the earlier TLS refresh showed that duplicate canonical content sources create drift.

## Implementation scope

Branch: `seo/agentsight-2026-09-14-session-ir`.

Rendered scope is intentionally limited to:

- new article route `/blog/how-agentsight-normalizes-agent-session-data/`;
- Blog hub card and CollectionPage membership;
- sitemap membership and 14 September freshness for the article and Blog hub;
- this operating report.

The implementation does not change workflows, analytics, shared-skill pointers, release metadata, existing canonical routes, or the operations schedule.

## Validation and delivery

Rendered PR `#127` (`https://github.com/eunomia-bpf/agentsight-website/pull/127`) final head `26afa398df27a5abed62cbb7b3afd7cb54974fdc` passed exact-head Website CI run `34869253587`, GitGuardian, and Cloudflare Pages. Copilot's three suppressed editorial/correctness observations were addressed in the final source: consumer mapping is described as current rather than future roadmap, token-component validation/fallback semantics match v1.0.31, and the provider-parser example is grammatically corrected. No unresolved review thread remained.

PR `#127` was squash-merged as `d7b00655ac5cfe21bc8251d0f2492bde8e935bd1`. Exact `Publish static site` run `34869461043` succeeded from that commit; its publish job completed at `2026-09-14T16:35:21Z` (09:35:21 PDT). Production `site/.source-sha` exactly matches that squash commit.

Generated production HTML contains the intended title, v1.0.31 description, index/follow robots metadata, and canonical `https://agentsight.us/blog/how-agentsight-normalizes-agent-session-data/`. A post-publication public re-verification ending at `2026-09-14T16:50:06Z` (09:50:06 PDT) successfully retrieved the article itself and the Blog hub, where the new card is present; the earlier local-session discovery article also remained retrievable as a representative unaffected route. The public article exposes the expected v1.0.31 research scope and primary-source links.

This publication satisfies the rolling 48-hour content SLO. It completed **24 hours 1 minute** before the previous `2026-09-15T16:36:21Z` deadline. The new rolling deadline is `2026-09-16T16:35:21Z` (09:35:21 PDT).

Metadata-only closeout PR `#128` (`https://github.com/eunomia-bpf/agentsight-website/pull/128`) records this post-merge evidence in the daily report and `status.md`. Its intended diff is limited to those two operating Markdown files; it changes no rendered site behavior, production route, workflow, analytics configuration, shared-skill pointer, or operations schedule. The closeout is complete only after its exact final head passes the expected checks, a from-scratch full-diff review is clean, and the PR is squash-merged.
