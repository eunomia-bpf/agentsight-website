# SEO operations — 2026-08-22

## Current cycle

- Site: `https://agentsight.us/`
- Product repository: `eunomia-bpf/agentsight`
- Website starting `main`: `8eb76c0e9692e0350e6933f532c217a8df9dce09`
- Product release at cycle start: website `v1.0.25`; authoritative product `v1.0.26`
- Authoritative product source commit: `92d634ec02d116a52acae62eb3b9b00c771e0b9d`
- Shared SEO skill pointer: `f42128a3f05c73cf10c786a2711c488bb3a14839`; allowed upstream `main` is unchanged at the same commit
- Cycle decision: repair material product-source drift; no separate content publication is required today
- Public URL removals, renames, redirects, canonical migrations, navigation removals, and analytics-policy changes: none

## Repository, product, and skill truth

No earlier open `seo/agentsight-` pull request was active at cycle start. The rendered repair used fresh branch `seo/agentsight-2026-08-22-v1-0-26-sync` from the then-current remote `main`.

The authoritative product `master` advanced from website-pinned `v1.0.25` commit `0080545f7c6b110ec2d4a4af5100b58f514c84d5` to release `v1.0.26` at `92d634ec02d116a52acae62eb3b9b00c771e0b9d`. The `v1.0.26` tag resolves in the product repository and `collector/Cargo.toml` declares version `1.0.26`.

The only substantive product change between the two release commits is merged product PR `#200`, **fix(web): make the recorded demo show complete AgentSight data**. That change fixes a product presentation defect rather than changing the underlying capture model:

- the recorded demo now loads a recorded `LiveOverview` together with the session snapshot instead of loading only the snapshot;
- coherent Codex and Claude fixture data exercises Overview, Conversation, Process Tree & AI Prompts, and Analysis;
- the sample includes process/resource state, tool calls, network targets, failures, plans, and source-reported subscriptions;
- offline conversation detail is derived from session-scoped captured PID activity; and
- Playwright coverage verifies the complete recorded-demo workflow.

The website therefore synchronizes current release identity and describes the v1.0.26 demo fix while preserving the v1.0.25 messaging-hardening history. It does not claim that every hosted app deployment has been independently verified at v1.0.26.

The shared SEO skill allowed branch still ends at `f42128a3f05c73cf10c786a2711c488bb3a14839`, exactly matching the website gitlink. No compatible skill update is available.

## Analytics implementation and publication source

Runtime GA4 remains present in `src/app/layout.tsx`. The configured page view sends `page_location` as origin plus pathname and `page_path` as pathname, so query strings remain excluded under the path-only policy. Google signals and ad-personalization signals remain disabled.

At cycle start, `site/.source-sha` identified `15e5cd2cfb9b7db5973ed5e2c9b5fd2dd6fac839`, the exact rendered commit for the 21 August overhead-methods article.

Independent canonical-homepage retrieval was fresh for the pre-repair v1.0.25 generation and confirmed that the release drift was publicly visible before the repair.

## Google Analytics and Search Console evidence

The configured Drive folder `agentsight.us SEO Weekly CSV` resolved uniquely and was inspected read-only.

- Configured finalization lag: three days.
- Latest fully finalized calendar date: `2026-08-19`.
- Present source manifests remain exactly:
  - `2026-07-27_to_2026-08-02_ga4_source.json`
  - `2026-08-03_to_2026-08-09_ga4_source.json`
  - `2026-08-10_to_2026-08-16_ga4_source.json`
- The newest manifest covers 10–16 August and was generated on 17 August.
- Direct-child inspection still finds no CSV files. There is no paired `*_ga4_organic_landing_pages.csv` for any manifest and no matching `*_gsc_*.csv` export.
- Both 3–9 August and 10–16 August are fully finalized weekly windows and remain incomplete.

The exporter-health failure is unchanged. GA4 and Search Console source-native metrics remain unavailable, not zero, and no valid period-over-period landing-page or query analysis can be computed. This runtime can inspect Drive artifacts but cannot execute or configure the external Google Apps Script exporter, so the existing human-only blocker remains accurate.

Cloudflare analytics remains not configured.

## Public search and off-site visibility

A bounded public scan checked the canonical domain, brand discovery, current AgentSight technical results, and the scoped npm package name.

The canonical homepage remains discoverable for the AgentSight brand, while unrelated products using the AgentSight name remain present in broad brand search. The AgentSight paper and project remain discoverable on independent academic/indexing surfaces. No new external page was verified to contain an actual backlink to `agentsight.us`, so no search-only result is promoted to a verified backlink.

A fresh scoped-package check still did not establish that `@eunomia-bpf/agentsight@1.0.0` is publicly installable. The npm first-publication blocker remains unresolved.

## Content-clock decision

The latest qualifying substantive publication is `/blog/how-much-overhead-does-agentsight-add/`, published at `2026-08-21T16:39:40Z` (09:39 PDT).

The next normal daily cycle is scheduled before the rolling 48-hour deadline from that publication. The v1.0.26 release synchronization is a factual repair and does not count as a qualifying substantive publication. No thin release article or duplicate demo page was justified today.

## Rendered repair delivered

Rendered PR `#77`, **Synchronize AgentSight website to v1.0.26**, was opened from exact head `d3a9fac9d03a97170e499df4cb50f0500b44d991`.

The exact-head Website CI run `32585928154` completed successfully. Its verify job passed checkout, the autonomous SEO scope guard, `npm ci`, `npm run verify`, and static artifact upload. Exact artifact `9479038054` has digest `sha256:dfa97b7dba29554e33190b197971db8c6d8bc27396fe599a47bf659d8a63893c`.

The static artifact was reviewed from scratch after CI:

- 38 normal generated index pages and 38 sitemap URLs, unchanged from the prior public route inventory;
- exactly one H1 and one canonical on every normal generated page;
- homepage output contains v1.0.26 and product commit `92d634ec02d116a52acae62eb3b9b00c771e0b9d`;
- Changelog contains the v1.0.26 recorded-demo fix and retains a separate v1.0.25 messaging-reliability entry;
- `llms.txt` reports v1.0.26 and preserves current extension/runtime boundaries;
- the overhead research article remains pinned to its deliberate v1.0.25 experiment/source scope; and
- GA4 remains present in representative generated HTML.

The final diff remained limited to six expected files: global release identity, Changelog, `llms.txt`, the 22 August daily report, `status.md`, and refreshed blocker evidence. No workflow, dependency, route, canonical, redirect, navigation, pricing, or analytics-policy file changed.

PR `#77` was squash-merged as `a1b21100ff12fe418a4777912583c3f6b8750da3` at `2026-08-22T16:52:41Z`.

The repository's exact static publication then updated the `site` branch with commit `20ffbe85967fc883540191abb29c4c961a19ae90`, authored at `2026-08-22T16:53:20Z` with message `Publish a1b21100ff12fe418a4777912583c3f6b8750da3`. `site/.source-sha` exactly matches `a1b21100ff12fe418a4777912583c3f6b8750da3`, so the publication source is proven to be the rendered squash commit.

The published `site` branch contains the new generated homepage and GA4 loader. Independent canonical-homepage crawler retrieval immediately after publication still returned its prior v1.0.25 cached generation. Because exact-head CI, generated artifact, publication branch, and `.source-sha` all agree on v1.0.26, this is recorded as crawler/CDN freshness rather than a rendered regression; no cache-forcing content edit was made.

Repository-local `npm run verify` could not be executed before the PR because the local sandbox could not resolve GitHub to obtain a checkout. No local success was fabricated; exact-head CI supplied the required repository validation.

## Closeout

The rendered change is complete. A metadata-only closeout PR updates this report and `status.md` with the exact CI/artifact/merge/publication/public-freshness facts. It must not alter rendered source, workflows, routes, analytics, dependencies, or the content clock.

## Human-only blockers

1. **Google SEO export pipeline:** two consecutive fully finalized weekly windows remain manifest-only. An authorized Apps Script operator must repair missing GA4/Search Console CSV generation and rerun a finalized window.
2. **npm scoped first publication:** an npm administrator must complete or authorize the first `@eunomia-bpf/agentsight` publication before the website may claim it as an install path.
