# SEO operations — 2026-08-09

## Current cycle

- Site: `https://agentsight.us/`
- Local timezone: `America/Los_Angeles`
- Cycle type: owner-directed full legacy-content quality upgrade and publication closeout
- Status: exact static publication complete; per-detail-route crawler freshness remains mixed
- Product release used for AgentSight-specific claims: `v1.0.3`
- Product source commit: `07a83a32257b8c8dcba911bd9db23f77e71dc085`
- Public URL removals, renames, redirects, and canonical migrations: none

## Owner direction and scope

The owner requested that all remaining thin/template-like legacy content be upgraded rather than selecting one page at a time. The work therefore treated the existing content portfolio as one coherent quality-remediation outcome while preserving every existing route.

The rendered upgrade covers:

- all four existing use-case detail pages;
- all five existing comparison pages;
- all three existing guide pages;
- the older `/blog/from-agent-trace-to-review-artifact/` article;
- all four existing integration pages;
- all five existing standalone search landing pages;
- the custom `/security/` page.

`/blog/system-boundary-observability/` already has a dedicated long-form renderer and was not rewritten. `/blog/why-ai-agent-tls-traffic-is-hard-to-trace/` was already a source-level long-form article and was not downgraded or replaced.

## Editorial result

The old generic pages previously shared a thin pattern of a lede, three outcomes, two short sections, a command block, and one or two links. PR `#44` adds an explicit deep-content upgrade for each of the 22 legacy generic pages.

Each upgraded generic page now has at least five substantive sections and at least two supporting source/reference links. The content is differentiated by reader decision rather than keyword substitution:

- use cases explain why and when to investigate a class of run;
- integrations document current runtime, binary, TLS, container, and native-telemetry compatibility boundaries;
- Claude/Codex observability landings describe the multi-layer observability stack instead of duplicating the integration pages;
- `/mcp-server-audit/` is a concrete, reproducible audit method using the AgentSight MCP fixture;
- comparison pages use current primary sources and state complementary use cases rather than forcing a winner/loser feature table;
- Agent Flamegraph now explains stack semantics, weighting modes, tagging reproducibility, drill-down, and limitations;
- the older review-artifact Blog now separates observed facts, deterministic summaries, inference, redaction, uncertainty, and reproduction;
- eBPF and file-access landings now explain the actual measurement mechanisms and capture limits rather than generic SEO copy.

The Security page now explicitly covers local-first storage versus sensitivity, privileged observation, targeted recording, export/OTel boundaries, redaction, absence uncertainty, reproducibility, and safe public reporting.

## Primary-source basis

AgentSight-specific behavior is grounded in the current `v1.0.3` source and documentation, including supported-agent/runtime notes, `agentpprof`, OpenTelemetry export, the MCP test fixture, the OpenClaw container experiment, `sslsniff`, and Codex/rustls offset detection.

External technical comparisons use current primary sources from OpenTelemetry, Claude Code, Gemini CLI, the open-source Codex OpenTelemetry implementation, Cloudflare AI Gateway, Langfuse, LangSmith, Model Context Protocol, and Linux kernel uprobe documentation. No customer, benchmark, compatibility, or roadmap claim was invented.

## Quality regression gate

`scripts/check-content.mjs` now requires all 22 legacy generic pages to retain an explicit deep-content upgrade. Each upgrade must contain at least five substantive section bodies and at least two supporting references. The existing route, title, slug, and internal-link checks remain active. This turns the new content floor into CI policy rather than a one-time rewrite.

## Exact delivery evidence

- Main content pull request: `#44`, `Raise all legacy content pages to the new quality baseline`
- PR base: `84e654946efa0f6338a2db2ff99d9c424b119b18`
- Exact final PR head: `a0b2dcd43a5c8b5463cbe10283b5f1293502a5b0`
- Changed rendered/validation files: `src/lib/public-content.ts`, `src/app/security/page.tsx`, `scripts/check-content.mjs`
- Exact-head Website CI: run `31300709260`, successful
- Exact static artifact: `9034396225`
- Artifact digest: `sha256:9d9ecaeee5310c31800150423e219aadedd334da366dd18558bcc084b182cf26`
- Squash commit: `f831d1c80be9285f2240ef7949a33ddc1a305806`
- Main Website CI: run `31300787290`, successful
- Publish static site: run `31300787279`, successful
- Publish job completed: `2026-08-09T07:17:27Z` (`2026-08-09 00:17:27` PDT)
- Publication marker: `site/.source-sha = f831d1c80be9285f2240ef7949a33ddc1a305806`

## Exact static-output review

The exact PR artifact contains 35 generated `index.html` pages and the existing 33 unique sitemap URLs. Every generated page has exactly one canonical and one H1.

Representative upgraded page bodies were inspected across every family. The 22 generic upgrades render approximately 350–575 article-body words each, with five to eight substantive content sections before the generic source/continuation UI. The expanded Security page renders approximately 760 words. The already-deep system-boundary and TLS articles remain substantially longer and retain their existing canonical ownership.

The exact generated public HTML contains no standalone abstract `evidence` marketing wording. Blog remains a first-class shared navigation destination and the Blog hub remains at three entries; this content-quality pass adds no new route.

## Public freshness qualification

Independent retrieval of the canonical homepage is fresh and exposes the current `v1.0.3` homepage and `Blog` navigation. Independent retrieval of individual detail routes remains inconsistent: one sampled slow-run URL returned an older cached `v0.2.67` generation while several other detail routes returned crawler cache misses. This does not match the exact `site` publication branch and is recorded as per-route crawler/cache freshness rather than a rendered-code regression, a 404, or successful direct verification.

Do not create another rendered change solely to force crawler refresh. Re-check the representative detail routes during a later operating cycle and use Search Console when valid exports are restored.

## Analytics state

This owner-directed closeout did not invent a new analytics window. The latest known analytics state remains the one recorded on `2026-08-08`: the configured Drive folder contained a GA4 source manifest without its required paired GA4 CSV and no matching Search Console CSV exports. Source-native traffic/search metrics therefore remain unavailable for comparison, not zero, and the external exporter repair remains tracked in `block.md`.

## Content clock

- Latest new Blog publication remains `/blog/why-ai-agent-tls-traffic-is-hard-to-trace/`, published at `2026-08-08 17:02` PDT.
- This full portfolio pass is also a qualifying major evergreen refresh under the repository's rolling 48-hour content policy.
- Latest qualifying substantive publication time: `2026-08-09 00:17` PDT, when the exact refreshed static site finished publication.
- A future cycle should continue the planned rotation toward a new research Blog or a real first-party experiment rather than immediately rewriting the same pages again.
