# 2026-08-27 AgentSight SEO operating record

## Scope and baseline

- Canonical site: `https://agentsight.us/`.
- The cycle first completed the carried metadata-only PR `#88`. Its exact head `4b50e015088f71769e02c2b6c22dba765ff8a523` now has successful Website CI run `32990458502`; final review confirmed only `block.md`, `daily/2026-08-26.md`, and `status.md` changed. PR `#88` was therefore squash-merged as `3479e3e50e2a1ad20f1ec5dae568d18f49b95fff`.
- Today’s content branch starts from that exact `main` commit.
- Shared SEO skills remain pinned to `f42128a3f05c73cf10c786a2711c488bb3a14839`; allowed upstream `main` is still the same commit.
- Authoritative AgentSight `master` remains exactly `934f441eff8ca210807333633f47b2efcb8cd020`, the v1.0.30 release commit. No product drift is present.

## Analytics, source freshness, and production health

- The configured Drive folder resolves uniquely as `agentsight.us SEO Weekly CSV` and still contains weekly artifact families only through 10–16 August.
- No `2026-08-17_to_2026-08-23` GA4 or Search Console family is present today. That window is already past the configured three-day finalization cutoff, so the Google exporter blocker remains active and current comparative analytics remain unavailable.
- The latest GA4 and GSC files are still next-morning snapshots generated on 17 August, before the site’s finalization lag. Their previously recorded directional values are not promoted to finalized KPIs.
- Cloudflare analytics remains disabled by repository configuration.
- Source inspection confirms GA4 remains path-only (`page_location` origin + pathname and `page_path` pathname) with Google signals and ad-personalization signals disabled.
- Direct public homepage retrieval today serves v1.0.30 and the expected current product surface. A generic search snapshot still exposes older v1.0.25 copy, so search-index freshness continues to lag production without establishing a production incident.
- Generic AgentSight search remains ambiguous with unrelated products. No new independently verified external link to an `agentsight.us` canonical was established.

## Content clock

- Previous qualifying publication: `/blog/observe-ai-agent-sessions-in-docker/`, published `2026-08-25T16:53:27Z` (09:53:27 PDT).
- Rolling 48-hour deadline: `2026-08-27T16:53:27Z` (09:53:27 PDT).
- No higher-priority production defect or product-fact drift was found today, so the content SLO becomes the active obligation. A metadata-only cycle would cross the deadline before the next normal operating cycle.

## Substantive outcome research brief

- Candidate title: **How to read AgentSight audit provenance and confidence**.
- Canonical route: `/blog/read-agentsight-audit-provenance/`.
- Reader problem: AgentSight can expose direct captured rows, rows reconstructed from persisted SQLite data, and rows parsed from native agent sessions. Reviewers and downstream tooling need to know whether similarly shaped rows are equivalent and whether confidence values can be compared across those sources.
- Decision supported: preserve `view_source` and source-specific `confidence` when exporting, deduplicating, comparing, or escalating AgentSight evidence; do not interpret confidence as a single globally calibrated probability.

### Semantic overlap and information gain

- The Product and Changelog surfaces mention audit provenance at a feature-summary level, and `status.md` records the durable product boundary.
- No indexed article currently owns the operational question of how to interpret `view_source`, why the same logical operation can produce multiple rows, how live LLM correlation scores are assigned, how persistence changes lineage, or how legacy artifacts should be handled.
- The new article adds source-level interpretation: the four provenance values; exact/heuristic/orphan request-response confidence paths; SQLite prompt reprojection; agent-native extraction; anti-forgery handling for payload fields that resemble reserved provenance; legacy schema fallbacks; the distinction between top-level `summary.source` and row-level lineage; and a safe snapshot-review workflow.

### Primary evidence

- AgentSight v1.0.30 `docs/snapshot-schema.md`: schema version 1, row provenance semantics, compatibility rules, and captured-content sensitivity.
- AgentSight v1.0.30 `ext/analysis/src/model.rs`: row-level `view_source` / `confidence` fields across audit, LLM, process, session, and tool models.
- AgentSight v1.0.30 `ext/analysis/src/view/projection.rs`: current live LLM matching rules — 0.95 exact request ID, 0.75 single pending request, 0.70 unique heuristic candidate, and 0.35 orphan response — plus bounded pending queues.
- AgentSight v1.0.30 `ext/analysis/src/sources/sqlite.rs`: SQLite reconstruction, 0.50 reconstructed prompt provenance, internal call-kind checks, and prompt deduplication behavior.
- AgentSight v1.0.30 `ext/analysis/src/sources/agent_native.rs`: native-session materialization and bounded session detail.
- Product PR `#204`: reviewed provenance implementation, persistence round trips, correlation-confidence preservation, and legacy-schema migration/fallback behavior.

### Claims explicitly avoided

- A `confidence` value is not described as a global probability or a cross-source ranking score.
- `view_source=view` is not described as proof that every semantic correlation is exact.
- Native agent history is not described as an independent OS-level trace.
- Agreement between two sources is not converted into an invented combined confidence score.
- Provenance fields are not described as a redaction or privacy mechanism.

## Delivery target

- Rendered scope: one new source-grounded article, Blog index entry, sitemap entry, and this operating record.
- Product facts remain pinned to AgentSight v1.0.30 commit `934f441eff8ca210807333633f47b2efcb8cd020`.
- Required acceptance: exact-head Website CI; from-scratch final diff review; squash merge; exact `Publish static site` success for the squash commit; generated-site source marker and canonical/sitemap checks; direct public route verification; then a metadata-only closeout update to this record and `status.md`.
