# SEO operations — 2026-08-18

## Current cycle

- Site: `https://agentsight.us/`
- Local timezone: `America/Los_Angeles`
- Cycle type: analytics/source patrol, product and production verification, public-search sampling, and substantive evergreen publication
- Starting website `main`: `3991c29f18de36d75996147e784fccc5df7a056a`
- Starting rendered website release: AgentSight `v1.0.25`
- Current authoritative product release: AgentSight `v1.0.25`
- Current product release commit: `0080545f7c6b110ec2d4a4af5100b58f514c84d5`
- Public URL removals, renames, redirects, canonical migrations, navigation changes, analytics-policy changes, and documentation-boundary changes: none

## Repository and product baseline

No earlier `seo/agentsight-` pull request remained open when this cycle started. The current website `main` already identifies AgentSight v1.0.25 and the authoritative product repository has no release or product commit newer than the v1.0.25 release commit. No product-source synchronization repair is required today.

The consuming repository pins shared SEO skills at `f42128a3f05c73cf10c786a2711c488bb3a14839`; the allowed upstream `main` is still the same commit, so no compatible submodule update is available. No `.github/seo-data/autoaction.md` exists, so repository mutation remains in the ordinary PR/CI/squash lane.

## Analytics and source data

Runtime GA4 remains configured in `src/app/layout.tsx` with the site-owned path-only policy:

- `page_location` is origin plus pathname;
- `page_path` is pathname;
- query strings are omitted;
- Google signals are disabled;
- ad-personalization signals are disabled.

The current static publication branch homepage still contains the expected Google Analytics loader and canonical metadata. Source and built output therefore remain aligned for runtime instrumentation.

The configured Drive folder still contains exactly three AgentSight GA4 source manifests and no usable CSV export:

- `2026-07-27_to_2026-08-02_ga4_source.json`, generated 8 August;
- `2026-08-03_to_2026-08-09_ga4_source.json`, generated 10 August;
- `2026-08-10_to_2026-08-16_ga4_source.json`, generated 17 August.

The newest manifest still identifies `agentsight.us` and is unchanged from the prior cycle; the previously recorded public-safe SHA-256 remains `f8d31e5f2ecc4f317ec000d977a004f691c12473a8579cd144a9ac6403f44b44`. None of the three manifests has its required paired `*_ga4_organic_landing_pages.csv`, and no matching Search Console CSV is present. Under the configured three-day finalization lag, the 10–16 August weekly range is not fully finalized today; the fully finalized 3–9 August window still has no usable GA4/GSC CSV. Source-native traffic metrics remain unavailable, not zero.

Cloudflare analytics remains disabled by site configuration.

## Public production and search visibility

Independent canonical-homepage retrieval is now fresh for the current v1.0.25 publication: the public page exposes the v1.0.25 announcement, current-release label, and product source pin `0080545f7c6b110ec2d4a4af5100b58f514c84d5`. This resolves the homepage freshness qualification recorded immediately after yesterday's publication.

Public search remains mixed because unrelated products use the AgentSight name, especially `agentsight.io`. The canonical `agentsight.us` homepage and the Eunomia open-source repository are discoverable, but public result composition is directional evidence rather than Search Console data. Direct retrieval of some older detail routes remains inconsistent or cache-stale; because the canonical homepage, exact publication branch, prior CI, and source marker agree, there is no reproducible site-owned routing defect to repair today.

No new independently verified backlink or citation was established in this cycle. Existing independent mentions remain contextual evidence rather than authoritative product documentation.

## Content clock and selection

The latest qualifying substantive publication before this cycle was the v1.0.24 Architecture evergreen refresh, published at `2026-08-16T16:46:13Z` (09:46 PDT). The rolling 48-hour deadline was reached during today's cycle, so a qualifying publication was required after higher-priority repair checks. A release-only or metadata-only change would not satisfy the content SLO.

The selected canonical is the existing `/guides/agent-flamegraph/`. Semantic-overlap review rejected a new Agent Flamegraph URL: the current guide already owned the reader decision, but its visible body still reflected the earlier generic/v1.0.3-era reference while the v1.0.25 product repository now exposes substantially richer reproducibility, privacy, weighting, stack-control, and tagging semantics.

### Reader question

How should an engineer build and interpret an Agent Flamegraph so that its widths, semantic categories, input sessions, and privacy boundary are reproducible rather than merely visually plausible?

### Durable information gain

The refresh is pinned to AgentSight v1.0.25 and adds current first-party implementation details that were not present in the existing guide:

- `agentpprof` is an offline/rootless profiler of local Codex and Claude Code history through `agent-session`; it is not a live eBPF or CPU profiler;
- explicit `--session-file` inputs are preferred over a drifting project-history scan for repeatable publication;
- operations, tokens, time, files, and network views have different width units and different failure modes;
- the default stack is the operation-field projection `task,skill,phase,action,object,repeat,result,outcome`, with `project`, `agent`, and `session` retained as pprof labels;
- `--op-map`, `--where`, `--stack`, and related controls are part of the reproducible method because they change how samples merge;
- token width prefers source-reported counts and bounds unsafe text estimates rather than allowing an implausible estimate to dominate the chart;
- JSON previews are opt-in and privacy-sensitive, while out-of-project paths are grouped into stable `external/*` buckets;
- the shipped iterative-tagging skill supplies concrete diagnostics for unmatched coverage and category distribution, which are workflow heuristics rather than statistical guarantees;
- the first-party v1.0.25 token SVG is embedded as a format example and explicitly not presented as a benchmark for another workload.

Primary sources are the exact v1.0.25 `ext/pprof/README.md`, `docs/agentpprof.md`, `skills/agentpprof-flamegraph/SKILL.md`, and the pinned first-party flamegraph gallery at product commit `0080545f7c6b110ec2d4a4af5100b58f514c84d5`.

## Rendered delivery

The smallest coherent search-facing outcome was a dedicated long-form renderer for the existing `/guides/agent-flamegraph/` canonical. It preserves the existing route, hub/card ownership, title, metadata path, navigation, analytics, product documentation boundary, and all other guide routes.

- Rendered PR: `#69`, **Refresh Agent Flamegraph guide for reproducible profiling**.
- Exact PR head: `2aba753c37bd83f53ebad02cddae80ebdb09e6b2`.
- Exact-head Website CI: `32162857964`, successful; the SEO scope guard, `npm ci`, `npm run verify`, static export, and artifact upload all completed successfully.
- Exact static artifact: `9334288217`, digest `sha256:7c3902555e98f1d71e7f3f2e8a94c4e819e8a8e393cda90bed6836a637094737`.
- Artifact review: 39 generated `index.html` files including the two error/not-found outputs, 37 normal generated index pages, and 37 unique sitemap URLs. Every normal generated index page has exactly one canonical and one H1.
- Target artifact: `/guides/agent-flamegraph/` renders about 1,650 whitespace-separated text tokens, 15 H2 sections, exactly one H1 and one canonical, the v1.0.25 / 18 August 2026 scope, all five view units/caveats, explicit `--session-file` reproducibility, operation stack/mapping/filter controls, `unknown=1` token-estimate behavior, tagging diagnostics, preview/privacy limits, exact v1.0.25 sources, and the pinned first-party token SVG.
- Representative unaffected artifact checks: homepage still contains GA4 and v1.0.25; Architecture retains one H1/canonical; the other guide pages are generated normally; sitemap cardinality is unchanged.
- PR `#69` was squash-merged as `b967206847915dc42535b127295647a239df2bc7`.
- The publication branch was updated by `github-actions[bot]` at `2026-08-18T16:58:09Z` with commit `349722f3971784392b34ed2a03ed2ef0eca81194`, message `Publish b967206847915dc42535b127295647a239df2bc7`.
- `site/.source-sha` exactly identifies `b967206847915dc42535b127295647a239df2bc7`, and the publication-branch target HTML contains the expected Agent Flamegraph canonical and refreshed output. The publish workflow itself is source-pinned and runs `npm ci` plus `npm run verify` before writing the `site` branch.

The execution sandbox could not obtain a local GitHub checkout before the PR because external DNS is unavailable there. No local-success claim was made; exact PR CI and exact publication performed the repository verification successfully before acceptance.

## Public acceptance

Immediately after exact publication, independent retrieval of `/guides/agent-flamegraph/` returned a crawler cache miss, so direct fresh-route acceptance is not claimed. Independent homepage retrieval is fresh for v1.0.25. Independent Architecture retrieval still exposes an explicitly four-day-old v1.0.15 crawl. Because exact PR CI, the reviewed artifact, the bot-authored publication commit, `site/.source-sha`, and publication-branch HTML all agree on the new canonical output, the detail-route result is recorded as crawler/index freshness rather than a rendered publication failure.

No rendered edit was created solely to force public crawler refresh. A later cycle should retry the refreshed guide and other stale detail routes; routing/cache investigation becomes warranted only if fresh independent live retrieval remains inconsistent while the publication branch stays current.

## Content clock closeout

The Agent Flamegraph evergreen refresh qualifies as the substantive publication for this cycle. Exact static publication completed at `2026-08-18T16:58:09Z` (09:58 PDT). This becomes the new rolling 48-hour content clock origin.

The next cycle should prefer a distinct evidence-producing result rather than another rewrite of the same guide. Strong candidates remain a real MCP-server audit, a reproducible slow/expensive-run breakdown from public-safe first-party sessions, or another deep implementation/reference refresh only when current product/source changes support a distinct reader question.

Rollback point for the rendered outcome: starting website `main` `3991c29f18de36d75996147e784fccc5df7a056a`.

## Blockers

- Google SEO export pipeline: unchanged genuine human-only blocker. The external job creates source manifests but continues to omit the required GA4 organic-landing CSV and all Search Console CSVs. This runtime can inspect Drive but cannot execute or configure the Apps Script exporter.
- Scoped npm first publication: unchanged genuine human-only blocker. Fresh public search still does not establish a published `@eunomia-bpf/agentsight` npm package, so the website continues to avoid that installability claim.

## Metadata closeout

This file and `status.md` are being finalized through a metadata-only closeout PR after the rendered publication. That PR must pass ordinary Website CI, receive a final complete-diff review, and squash-merge without changing rendered source. Its own normal publication workflow may run, but because rendered output is unchanged the `site` branch source marker should remain on the rendered commit rather than move to metadata-only state.
