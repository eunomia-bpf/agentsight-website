# 2026-08-25 AgentSight SEO operating record

## Scope and baseline

- Canonical site: `https://agentsight.us/`.
- Operating branch started from website `main` commit `8e7969984861a044d9152de85452432def5bcfc2`.
- No prior open `seo/agentsight-` pull request was present at cycle start.
- Shared SEO skills remain pinned to `f42128a3f05c73cf10c786a2711c488bb3a14839`; allowed upstream `main` has not moved.
- The prior qualifying substantive publication was `/blog/replay-coding-agent-repository-changes/`, published at `2026-08-23T16:37:33Z` (09:37 PDT). Its 48-hour deadline fell at `2026-08-25T16:37:33Z` during this operating cycle.

## Product drift

- Authoritative AgentSight `master` is `934f441eff8ca210807333633f47b2efcb8cd020` and the current immutable release is **v1.0.30**, published 25 August 2026.
- v1.0.29 documents the official `eunomia-bpf/tap` Homebrew installation path for Linux x86-64 through product PR `#196`.
- v1.0.30 adds a repository-maintenance bridge for shared agent skills through product PR `#206`: it pins `eunomia-bpf/agent-skills` under `.agents/sources`, adds small Bash and PowerShell sync entrypoints, and keeps generated `.agents/skills` links out of Git. The PR explicitly does not change product runtime code or existing repository-specific skills.
- The website started this cycle at v1.0.28, so release identity was a confirmed factual drift and was repaired before the substantive content outcome.

## Analytics and search evidence

- The configured Drive folder resolves uniquely and still contains the 3–9 August and 10–16 August GA4/GSC artifact families.
- No `2026-08-17_to_2026-08-23` weekly export is visible today. Under the configured three-day finalization lag that window does not become final until 26 August, so the absence is not treated as a defect.
- The 10–16 August GA4 source manifest confirms it was generated on 17 August, before the configured three-day lag. The existing exporter-timing blocker therefore remains valid; pre-finalization snapshots stay directional only.
- Cloudflare analytics remains disabled by repository configuration.
- Public retrieval of `https://agentsight.us/` before the release repair exposed the v1.0.28 homepage generation, so the crawler/CDN freshness qualification recorded on 24 August had resolved for the homepage.
- Public brand search also surfaces an unrelated Shopify app named “AgentSight: AI Agent SEO”, launched 24 July 2026. This is recorded as brand-search ambiguity, not as a verified backlink, endorsement, ranking cause, or site-owned technical defect.
- No new independently verified external link to an `agentsight.us` canonical was established in this patrol.

## Product release repair

- Release-sync PR: `#84`, `https://github.com/eunomia-bpf/agentsight-website/pull/84`.
- Exact final head: `d49f5d738042b484d6920266b0cf28401c3deff8`.
- Exact-head Website CI run `32873776714` completed successfully: autonomous SEO scope guard, `npm ci`, `npm run verify`, static export, and artifact upload all passed.
- From-scratch final review confirmed a five-file factual sync only: global release/source identity, Changelog, `llms.txt`, today’s operating record, and durable status. Existing `/releases/` Homebrew contribution and unrelated rendered behavior were preserved.
- PR `#84` was squash-merged as `9e22a1aa8f6dd28091843f5041a795144cb2b492`.
- Exact `Publish static site` run `32873880632` used `main` / squash commit `9e22a1aa8f6dd28091843f5041a795144cb2b492` and completed successfully at `2026-08-25T16:46:31Z`.
- Publication branch `site/.source-sha` exactly equals `9e22a1aa8f6dd28091843f5041a795144cb2b492`.
- Immediate independent crawler retrieval after publication still returned the pre-publication v1.0.28 generation. Exact build/publication evidence is internally consistent and this matches the short-lived crawler/CDN freshness behavior observed on 24 August; retain the qualification and recheck rather than creating a cache-forcing change.

## Content SLO

- The 48-hour deadline passed while the higher-priority confirmed product-release drift was being reviewed, merged, and published. The release publication itself completed at `2026-08-25T16:46:31Z`, about nine minutes after the substantive-content deadline.
- This cycle therefore records a **content-SLO miss** rather than lowering quality or publishing filler. The substantive outcome remains required and is being delivered in the same cycle.

## Substantive outcome research brief

- Working title: **How AgentSight observes AI agent sessions inside Docker**.
- Canonical route: `/blog/observe-ai-agent-sessions-in-docker/`.
- Reader problem: a development container can hold the authoritative native agent history, provider CLI state, and credentials while the AgentSight Node/UI runs on the host. The reader needs to understand how `agentsight bind --docker-container NAME` crosses that boundary and which trust assumptions remain.
- Decision supported: use Docker-backed `bind` to expose existing native session state from a trusted named container without copying provider credentials to the host; treat Docker daemon access as a separate broad host authority; use Linux `record`/tracing when the question is independent system-level effects rather than native session history.

### Semantic overlap and information gain

- Existing Product, Architecture, Changelog, and `llms.txt` summarize the Docker-backed session feature and its high-level trust boundary.
- No dedicated indexed page currently owns the source-level reader question above.
- The new article adds information not present in those summaries: the three-operation JSONL protocol; exact frame/message/time limits; one bridge process per configured container; user/workdir/home reconstruction; multi-container conflict handling; fail-closed lookup when a peer is unavailable; saved-DB/live-source exclusion; the Codex external-sandbox consequence; and the distinction between the AgentSight named-container allowlist and underlying Docker daemon authority.

### Primary evidence

- AgentSight v1.0.30 `docs/usage.md`: operator contract, supported native providers, credential locality, Codex container sandbox boundary, Docker socket warning, version matching, labels/fallbacks.
- AgentSight v1.0.30 `collector/src/server/container_bridge.rs`: JSONL methods, 8 MiB frame cap, 30-second operation timeout, one-second bridge lock timeout, 10-second Docker command timeout, 65,536-byte message cap, multi-container fan-out, conflict/error behavior, validated `docker exec -i` construction.
- AgentSight v1.0.30 `collector/src/cmd_bind.rs`: saved captures reject Docker live session sources because saved captures are read-only.
- Product PR `#195`: implementation and validation history for Docker-backed native sessions.
- Docker official `docker exec` documentation: user/workdir/environment execution controls.
- Docker official security documentation: control of the Docker daemon is a high-authority host capability; rootless operation is a distinct mitigation boundary.

### Claims explicitly avoided

- Docker-backed native sessions are not described as an independent system trace of every process/file/network effect.
- Keeping provider credentials inside the container is not described as keeping all session content inside the container; parsed session evidence intentionally crosses the bridge to the host Node.
- `--docker-container` is not described as least-privilege Docker authorization; it constrains AgentSight behavior on top of the Docker authority already granted to the host client.
- Cursor is not described as resumable through this path in v1.0.30; it remains observation-only.
- The article does not generalize the bridge to every agent provider or every container runtime.

## Delivery state

- Release repair is merged and exactly published as recorded above.
- The substantive Docker-session article is being delivered from a fresh branch based on release-sync squash commit `9e22a1aa8f6dd28091843f5041a795144cb2b492`.
- Final article PR, exact-head CI, squash commit, publication, public acceptance, and metadata closeout are recorded after they become terminal.
