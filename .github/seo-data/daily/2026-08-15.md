# SEO operations — 2026-08-15

## Current cycle

- Site: `https://agentsight.us/`
- Local timezone: `America/Los_Angeles`
- Cycle type: product-drift repair, source/data patrol, public-search verification, and release synchronization
- Starting website main: `8779f849b2c369e4fbfc909b60db0a86b7c07c4e`
- Starting rendered website release: AgentSight `v1.0.21`
- Current authoritative product release: AgentSight `v1.0.22`
- Current product release commit: `4a789dc90942efaa09a21be4b83e0dec3629bce8`
- Public URL removals, renames, redirects, canonical migrations, navigation changes, analytics-policy changes, and documentation-boundary changes: none

## Product-source drift

Fresh GitHub Releases and product-branch inspection found `v1.0.22`, published on 15 August 2026 from product commit `4a789dc90942efaa09a21be4b83e0dec3629bce8`.

The release is intentionally narrow. The only release-note change is product PR `#186`, **chore: clean up local environment files**. That PR explicitly classifies its scope as repository hygiene with no runtime or product behavior change: it removes a committed local `.env`, ignores local environment variants while preserving an example file, adds an `.env.example` placeholder for optional Langfuse configuration, and removes a duplicate log-ignore block. The release commit also bumps package versions and dependency lock entries.

Therefore today's site repair is a factual current-release synchronization, not a new feature page or a qualifying substantive content publication. The v1.0.21 fleet, session-analysis, sign-in, Direct/Relay, and Controller behavior documented on the current Product and Architecture pages remains the current user-facing model.

## Rendered repair

The rendered repair was delivered through PR `#61`, **Sync AgentSight v1.0.22 release identity**, from `seo/agentsight-2026-08-15-v1-0-22-release-sync`.

The narrow repair updates:

- shared current release metadata from `v1.0.21` / `412ee6d...` to `v1.0.22` / `4a789dc...`;
- the Changelog current-release entry to state that v1.0.22 is hygiene-only and does not invent product behavior;
- `llms.txt` current-release identity while retaining the v1.0.21 user-facing behavior as the current product model;
- this daily operating record and `status.md`.

Product, Architecture, Pricing, navigation, canonical paths, redirects, GA4 configuration, deployment configuration, and canonical documentation ownership are preserved.

## Analytics and source data

Runtime GA4 remains configured in `src/app/layout.tsx` with the expected public measurement loader and the site-owned path-only policy:

- `page_location` is origin plus pathname;
- `page_path` is pathname;
- query strings are omitted;
- Google signals are disabled;
- ad-personalization signals are disabled.

The published static homepage also contains the GA loader and measurement ID, so source and generated publication output agree.

The configured Google Drive folder still contains only the two known GA4 source manifests:

- `2026-07-27_to_2026-08-02_ga4_source.json`;
- `2026-08-03_to_2026-08-09_ga4_source.json`.

The latter was generated on 10 August 2026 for the `agentsight.us` domain and covers the finalized `2026-08-03` through `2026-08-09` window. Neither manifest has the required paired `*_ga4_organic_landing_pages.csv`, and no matching Search Console CSV is present. This remains an exporter-health failure. GA4 and Search Console source-native metrics are unavailable, not zero, and no period-over-period comparison is valid.

Cloudflare analytics remains disabled by site configuration.

## Public search, production, and off-site visibility

Before the repair, direct public retrieval of the canonical homepage was fresh for the v1.0.21 baseline: it exposed the v1.0.21 release banner, release card, and source pin `412ee6dbad967fa48a226a8052ee0c689e4bd1c6`. This resolved the crawler/CDN freshness qualification recorded immediately after the prior publication.

Public search remains materially slower than direct production retrieval: sampled indexed homepage results still expose older positioning. Treat this as directional index freshness, not current production truth.

A bounded off-site scan found an independent Alibaba Cloud Linux documentation page, **How to use AgentSight**, last updated 20 June 2026. It describes an Alibaba Cloud Linux/ANOLISA AgentSight surface with token analysis, behavior audit, a dashboard, OpenClaw and Copilot Shell support, and commands such as `agentsight trace`, `serve`, `token`, `audit`, and `discover`. The page is a verified independent brand mention, but it is not recorded as a backlink: the inspected page exposes only Alibaba documentation navigation/Quick Start links and no verified link to `agentsight.us` or the current upstream repository. Its command and deployment surface also differs from current upstream open-source AgentSight, so it should not be used as authoritative product documentation for this website.

No other new independent external link to `agentsight.us` was verified in this scan.

## Shared SEO skill

- Consuming-repository gitlink: `f42128a3f05c73cf10c786a2711c488bb3a14839`.
- Current allowed upstream `main`: the same commit.
- No submodule update is available.

## Content clock

The latest qualifying substantive publication remains the v1.0.21 Product/Architecture evergreen refresh, whose exact static publication completed at `2026-08-14T17:12:35Z`.

Today's v1.0.22 synchronization is release metadata and factual maintenance only and does not reset that clock. Waiting until the next normal daily cycle remains below the 48-hour threshold, so no additional article or major evergreen refresh is required today. Publishing a thin v1.0.22 page would violate the content-quality policy because the release has no new user-facing behavior.

## Blockers

- Google SEO export pipeline: unchanged genuine human-only blocker. Finalized GA4/GSC CSV output is still absent, and this runtime cannot execute or configure the external Apps Script exporter.
- Scoped npm first publication: unchanged genuine human-only blocker. A fresh public package search did not establish a published `@eunomia-bpf/agentsight` npm package, so the website must continue to avoid that installability claim.

## Validation and delivery outcome

A local repository checkout was attempted before opening PR `#61` so `npm run verify` could run, but the execution environment could not resolve `github.com`; no local-check success is claimed. This did not become a delivery blocker because the repository's exact-head CI ran the authoritative checkout and verification path successfully.

PR `#61` final head was `615690bc1cb769714b251b3eb9571661c11eddd4`.

- Exact-head Website CI: `31896164711`, successful.
- Exact static artifact: `9249851468`.
- Artifact digest: `sha256:ac06c93c2467ce46a2e3f139fa502e2c188f99b82e3dac67645af6ab24ba8111`.
- Post-CI from-scratch review: clean; complete diff, PR metadata, product release evidence, zero review comments, and exact static artifact were re-inspected.
- Static artifact review: 39 generated index pages, 37 unique sitemap URLs, exactly one canonical and one H1 on every generated page, v1.0.22 homepage release identity, product source pin `4a789dc...`, retained GA loader, hygiene-only Changelog wording, and no scoped npm installability claim.
- PR `#61` squash/main commit: `f1f990bd4ac2854ceb727ac5a49d690e020d093a`.
- Main Website CI: `31896250720`, successful.
- Publish static site: `31896250693`, successful.
- Exact publication completed at `2026-08-15T16:43:09Z`.
- `site/.source-sha` exactly matches `f1f990bd4ac2854ceb727ac5a49d690e020d093a`.
- The publication branch homepage contains v1.0.22, the product source pin `4a789dc90942efaa09a21be4b83e0dec3629bce8`, the existing canonical, and the GA loader.

Independent canonical-homepage retrieval immediately after exact publication still returned the pre-publication v1.0.21 generation. Because the exact main commit, CI, generated artifact, publication workflow, and `site/.source-sha` all agree, this is recorded as a public-crawler/CDN freshness qualification rather than evidence of a failed static build. No rendered cache-forcing change is justified from this observation alone. A later operating cycle should recheck the canonical URL and escalate only if the mismatch persists independently of crawler caching.

This metadata closeout changes operating records only; it does not change rendered site output.
